Open `MagiMdmParent` in Xcode 15+, bundle id `com.zigmdm.parent`, iOS 17.
Add the Swift files to a new App project if the `.xcodeproj` is not committed.
Info.plist: allow local network / arbitrary loads only for LAN `mdm.home` if you use http.
